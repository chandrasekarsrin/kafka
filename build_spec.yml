version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
env:
  variables:
    "testEnv" : "testValue1"
  exportedVariables:
    - testEnv
    
    
steps:
  - type: Command
    name: "Build Source"
    timeoutInSeconds: 400
    command: |
      echo $testEnv
      gradle jar
      
outputArtifacts:
  - name: output1
    type: BINARY
    location: build/libs/kafka-3.0.0-SNAPSHOT.jar
